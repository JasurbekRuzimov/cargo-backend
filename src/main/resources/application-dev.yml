spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/postgres
    username: postgres
    password: Jasurbek_2105
    driver-class-name: org.postgresql.Driver
  jpa:
    hibernate:
      ddl-auto: create
    show-sql: false
    properties:
      hibernate:
        format_sql: true
    database: postgresql
    database-platform: org.hibernate.dialect.PostgreSQLDialect
  servlet:
    multipart:
      max-file-size:
        10MB
      max-request-size:
        10MB

server:
  port:
    8080
  address:
    localhost
app:
  url:
    http://${server.address}:${server.port}


jwt:
  secret: Bto6p+zSi8W0H2xnsMsx1oZ6km8Q/uOxI1wndpbuebE=
  expiration-ms: 604800000